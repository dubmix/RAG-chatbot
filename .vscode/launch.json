{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Backend",
            "type": "debugpy",
            "request": "launch",
            "module": "main",
            "cwd": "${workspaceFolder}/api",
            "justMyCode": true,
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}